# Strands Glue Pipeline Agent

This frontend talks to the agent service over the v1 SSE protocol. Type a request below to start.

Default agent backend: `http://127.0.0.1:8080`. Override with `AGENT_BASE_URL`.

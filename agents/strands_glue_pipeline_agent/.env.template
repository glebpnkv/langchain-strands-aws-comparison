# --- AWS ---
AWS_PROFILE=""
AWS_REGION=""

# --- Model ---
MODEL_ID="eu.anthropic.claude-haiku-4-5-20251001-v1:0"

# --- Glue runtime defaults ---
GLUE_JOB_ROLE_ARN=""
# IAM role assumed by EventBridge Scheduler targets when invoking
# athena:StartQueryExecution on behalf of the agent.
SCHEDULER_ATHENA_EXEC_ROLE_ARN=""
# TODO: move to conversation-scoped script path:
# s3://glue-assets-554032904022-eu-central-1-an/strands-glue-pipeline-agent/scripts/<conversation_id>/<script_name>.py
GLUE_JOB_DEFAULT_SCRIPT_S3=""
GLUE_TEMP_DIR=""

# --- Athena input (optional; provide either this or RAW_DATA_BUCKET_S3_URI, or specify per-conversation) ---
ATHENA_DATABASE=""
ATHENA_TABLE=""

# --- GitHub integration (REQUIRED) ---
# Fine-grained PAT, scoped to the single target repo.
# See README.md for the exact PAT creation clickthrough.
GITHUB_PAT=""
TARGET_REPO_OWNER=""
TARGET_REPO_NAME=""
TARGET_REPO_DEFAULT_BRANCH="main"

# --- Raw S3 ingestion (OPTIONAL) ---
# When set, enables the raw-S3 discovery flow as an alternative to Athena.
RAW_DATA_BUCKET_S3_URI=""
